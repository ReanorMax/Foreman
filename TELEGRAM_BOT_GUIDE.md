# Гайд по Telegram боту на сервере GitLab

Краткое руководство для коллеги по работе с Python Telegram ботом на сервере `10.19.1.20`.

## 🔐 Доступ к серверу

- **Сервер**: `10.19.1.20` (GitLab сервер)
- **Пользователь**: `sphere`
- **Пароль**: `Q1w2a3s40007`
- **SSH команда**: 
  ```bash
  ssh sphere@10.19.1.20
  ```

---

## ⚠️ ВАЖНО: Telegram бот не найден в сервисах

**Проверено на сервере 10.19.1.20:**
- ❌ Telegram бот **НЕ** найден в systemd сервисах
- ❌ Telegram бот **НЕ** запущен как процесс
- ❌ Telegram бот **НЕ** найден в crontab
- ❌ Telegram бот **НЕ** найден в screen/tmux сессиях
- ❌ Telegram бот **НЕ** найден в GitLab проектах

**Возможные варианты:**
1. Бот запускается вручную (не автоматически)
2. Бот находится на другом сервере
3. Бот запускается через GitLab Runner (как CI/CD задача)
4. Бот не настроен как сервис

**⚠️ ОБЯЗАТЕЛЬНО уточните у передающего коллеги:**
- Где именно находится код бота?
- Как он запускается?
- На каком сервере он работает?

---

## 📍 Расположение бота

**⚠️ ВАЖНО: Нужно уточнить у передающего коллеги точное расположение файлов бота!**

**Проверенные места (бот не найден):**
- `/home/sphere/` - проверено, файлов бота нет
- `/opt/` - проверено, файлов бота нет
- `/var/opt/` - проверено, файлов бота нет
- Systemd сервисы - проверено, сервиса бота нет
- Процессы Python - только системные процессы
- Crontab - пусто
- Screen/Tmux сессии - нет активных сессий

Типичные места расположения:
- `/home/sphere/telegram_bot/` или `/home/sphere/bot/`
- `/opt/telegram_bot/` или `/opt/bot/`
- `/var/opt/telegram_bot/`

**Для поиска бота на сервере:**
```bash
# Поиск файлов Python бота
find /home /opt /var -name "*.py" -type f 2>/dev/null | xargs grep -l "telegram\|Bot\|TOKEN" 2>/dev/null

# Поиск по имени файла
find /home /opt /var -name "*bot*.py" -o -name "*telegram*.py" 2>/dev/null

# Поиск в домашней директории пользователя
find ~ -name "*.py" -type f 2>/dev/null
```

---

## 🚀 Запуск бота

### Способ 1: Через systemd (рекомендуется)

```bash
# Проверить статус сервиса
systemctl status telegram-bot
# или
systemctl status bot
# или другое имя сервиса

# Запустить сервис
sudo systemctl start telegram-bot

# Остановить сервис
sudo systemctl stop telegram-bot

# Перезапустить сервис
sudo systemctl restart telegram-bot

# Включить автозапуск
sudo systemctl enable telegram-bot

# Посмотреть логи
journalctl -u telegram-bot -f
# или
sudo journalctl -u telegram-bot -n 50
```

**Если сервис не найден**, проверьте:
```bash
# Список всех сервисов
systemctl list-units --type=service --all | grep -i bot
systemctl list-units --type=service --all | grep -i telegram
systemctl list-units --type=service --all | grep python
```

### Способ 2: Вручную через Python

```bash
# Перейти в директорию бота
cd /path/to/bot/directory

# Запустить бота
python3 bot.py
# или
python3 main.py
# или
python3 telegram_bot.py

# Для запуска в фоне (nohup)
nohup python3 bot.py > bot.log 2>&1 &

# Для запуска через screen (сохраняет сессию)
screen -S telegram_bot
python3 bot.py
# Нажать Ctrl+A затем D для отключения
# Вернуться: screen -r telegram_bot

# Для запуска через tmux
tmux new -s telegram_bot
python3 bot.py
# Нажать Ctrl+B затем D для отключения
# Вернуться: tmux attach -t telegram_bot
```

### Способ 3: Через virtualenv

```bash
# Активировать виртуальное окружение
source venv/bin/activate
# или
source .venv/bin/activate

# Запустить бота
python bot.py
```

---

## 📋 Что делает бот

**⚠️ ВАЖНО: Нужно уточнить у передающего коллеги функциональность бота!**

Типичные функции Telegram ботов:

1. **Мониторинг серверов/систем**
   - Получение уведомлений о статусе сервисов
   - Отправка алертов при ошибках
   - Мониторинг дискового пространства, памяти, CPU

2. **Управление GitLab**
   - Уведомления о новых коммитах
   - Статус CI/CD пайплайнов
   - Создание/закрытие issues

3. **Автоматизация задач**
   - Запуск скриптов по команде
   - Управление резервными копиями
   - Выполнение cron-задач

4. **Информационные команды**
   - Получение статуса серверов
   - Просмотр логов
   - Системная информация

**Для определения функций бота:**
```bash
# Просмотреть код бота (замените путь на реальный)
cat /path/to/bot/bot.py
# или
cat /path/to/bot/main.py

# Поиск обработчиков команд (обычно @bot.message_handler или async def)
grep -n "message_handler\|def.*command\|async def" /path/to/bot/*.py
```

---

## 🔧 Конфигурация бота

### Токен Telegram бота

Токен обычно хранится в:
- Файле конфигурации (`.env`, `config.py`, `settings.py`)
- Переменных окружения
- Файле `.env` в директории бота

**Поиск токена:**
```bash
# Поиск в файлах конфигурации
find /path/to/bot -name ".env" -o -name "config.py" -o -name "settings.py" 2>/dev/null

# Поиск токена в коде
grep -r "TOKEN\|token\|Token" /path/to/bot/ 2>/dev/null | grep -v ".pyc"
```

**Пример файла конфигурации `.env`:**
```bash
TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
CHAT_ID=123456789
```

**Пример использования в Python:**
```python
import os
from dotenv import load_dotenv

load_dotenv()
TOKEN = os.getenv('TELEGRAM_BOT_TOKEN')
CHAT_ID = os.getenv('CHAT_ID')
```

---

## 📦 Зависимости (requirements.txt)

**Проверка зависимостей:**
```bash
# В директории бота
cat requirements.txt
# или
cat pip-requirements.txt
```

**Типичные зависимости для Telegram ботов:**
```
python-telegram-bot>=20.0
# или
pyTelegramBotAPI>=4.0
# или
aiogram>=3.0

python-dotenv>=0.19.0
requests>=2.28.0
```

**Установка зависимостей:**
```bash
# Установка всех зависимостей
pip3 install -r requirements.txt

# Или в virtualenv
source venv/bin/activate
pip install -r requirements.txt
```

---

## 🔄 Управление ботом

### Проверка статуса бота

```bash
# Проверить, запущен ли процесс
ps aux | grep -i "telegram\|bot\|python.*bot" | grep -v grep

# Проверить через systemd
systemctl status telegram-bot

# Проверить логи
tail -f /var/log/telegram_bot.log
# или
tail -f ~/bot.log
# или
journalctl -u telegram-bot -f
```

### Остановка бота

```bash
# Через systemd
sudo systemctl stop telegram-bot

# Найти процесс и остановить вручную
ps aux | grep python.*bot
kill <PID>
# или принудительно
kill -9 <PID>
```

### Перезапуск бота

```bash
# Через systemd
sudo systemctl restart telegram-bot

# Вручную
# Сначала остановить, затем запустить снова
```

---

## 📝 Логи

**Типичные места логов:**
- `/var/log/telegram_bot.log`
- `~/bot.log` или `/home/sphere/bot.log`
- `journalctl` (если запущен через systemd)
- Вывод в терминале (если запущен вручную)

**Просмотр логов:**
```bash
# Последние 50 строк
tail -50 /var/log/telegram_bot.log

# Следить за логами в реальном времени
tail -f /var/log/telegram_bot.log

# Поиск ошибок
grep -i error /var/log/telegram_bot.log
grep -i exception /var/log/telegram_bot.log
```

---

## ❌ Типичные проблемы и решения

### Бот не запускается

```bash
# 1. Проверить зависимости
pip3 list | grep telegram

# 2. Проверить токен
grep -r "TOKEN" /path/to/bot/ | head -1

# 3. Проверить права доступа
ls -la /path/to/bot/
chmod +x /path/to/bot/bot.py

# 4. Проверить Python версию
python3 --version
# Должна быть 3.7+

# 5. Запустить с выводом ошибок
python3 bot.py 2>&1 | tee bot_errors.log
```

### Бот не отвечает на команды

1. Проверить, что бот запущен
2. Проверить токен (правильный ли)
3. Проверить логи на ошибки
4. Проверить, что бот добавлен в Telegram (через @BotFather)

### Ошибка "Token is invalid"

1. Проверить токен в `.env` или `config.py`
2. Обновить токен через @BotFather в Telegram
3. Перезапустить бота

### Ошибка "Module not found"

```bash
# Установить недостающие модули
pip3 install <module_name>
# или
pip3 install -r requirements.txt
```

---

## 🛠️ Настройка автозапуска

### Через systemd (рекомендуется)

Создать файл `/etc/systemd/system/telegram-bot.service`:

```ini
[Unit]
Description=Telegram Bot
After=network.target

[Service]
Type=simple
User=sphere
WorkingDirectory=/path/to/bot
ExecStart=/usr/bin/python3 /path/to/bot/bot.py
Restart=always
RestartSec=10
StandardOutput=append:/var/log/telegram_bot.log
StandardError=append:/var/log/telegram_bot.log

[Install]
WantedBy=multi-user.target
```

**Команды:**
```bash
# Перезагрузить systemd
sudo systemctl daemon-reload

# Включить автозапуск
sudo systemctl enable telegram-bot

# Запустить сервис
sudo systemctl start telegram-bot
```

### Через crontab

```bash
# Редактировать crontab
crontab -e

# Добавить строку (запуск при загрузке системы)
@reboot cd /path/to/bot && /usr/bin/python3 bot.py >> /var/log/bot.log 2>&1

# Или запуск каждый раз при падении (через скрипт-обертку)
@reboot /path/to/scripts/start_bot.sh
```

---

## 🔍 Полезные команды

```bash
# Проверить версию Python
python3 --version

# Проверить установленные пакеты
pip3 list | grep telegram

# Проверить активные сетевые подключения бота
netstat -tulpn | grep python
ss -tulpn | grep python

# Проверить использование ресурсов
top -p $(pgrep -f "python.*bot")
htop -p $(pgrep -f "python.*bot")

# Проверить права доступа
ls -la /path/to/bot/
```

---

## 📞 Информация для передачи дел

**Перед передачей дел уточните у коллеги:**

1. ✅ Где находится код бота? (точный путь)
2. ✅ Как запускается бот? (systemd/cron/вручную)
3. ✅ Что делает бот? (какие команды обрабатывает)
4. ✅ Где хранится токен? (файл .env/config.py)
5. ✅ Какие зависимости? (файл requirements.txt)
6. ✅ Где логи? (путь к файлу логов)
7. ✅ Есть ли автозапуск? (systemd/crontab)

**После получения информации - дополните эту документацию конкретными путями и командами!**

---

## 📝 Чек-лист для коллеги

При принятии дел проверьте:

- [ ] Бот запущен и работает (`ps aux | grep bot`)
- [ ] Бот отвечает в Telegram
- [ ] Логи доступны и нет ошибок
- [ ] Автозапуск настроен (systemd/crontab)
- [ ] Токен бота сохранен в безопасном месте
- [ ] Документация обновлена с реальными путями
- [ ] Знаете, как перезапустить бота
- [ ] Знаете, где логи
- [ ] Знаете, как проверить статус

---

**⚠️ ВАЖНО: Эта документация - шаблон. После получения информации от передающего коллеги дополните её конкретными путями, командами и функциональностью бота!**

