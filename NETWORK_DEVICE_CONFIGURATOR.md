# Проект Network Device Configurator (NDC)

Документация по проекту `Automatization/network-device-configurator` для передачи дел коллеге.

---

## 📋 Общая информация

### Проект

- **Название**: `network-device-configurator` (NDC)
- **GitLab URL**: `http://10.19.1.20/Automatization/network-device-configurator`
- **ID проекта**: `98`
- **Репозиторий**: `http://10.19.1.20/Automatization/network-device-configurator.git`
- **Создан**: `2025-09-18`
- **Версия**: `v2.0.0` (последний релиз)
- **Ветка по умолчанию**: `main` (защищена)

---

## 💡 Что делает проект

### Назначение

**Network Device Configurator (NDC)** - система автоматической конфигурации сетевого оборудования для производственных нужд.

**Поддерживаемые устройства:**
- **Grandstream HT812** - VoIP-адаптеры (аналоговые телефонные адаптеры)
- **MikroTik** - WiFi точки доступа и маршрутизаторы
- **МАВР** - специализированные устройства (АВУ, АВК) с Wi-Fi модулями

### Основные возможности

✅ **Автоматическое обнаружение устройств** - сканирование DHCP-аренды MikroTik  
✅ **Массовая конфигурация** - настройка через SSH и TFTP  
✅ **Отслеживание статуса** - контроль процесса конфигурации  
✅ **Ведение логов** - запись в файлы и базу данных MySQL  
✅ **Интерактивные меню** - удобный интерфейс для различных типов устройств  
✅ **Поддержка базы данных** - переход от файлового хранения к MySQL  

---

## 🌿 Ветки проекта

### Ветка `main` (защищена)

**Статус**: По умолчанию, защищена от прямого пуша  
**Назначение**: Основная стабильная версия проекта  
**Версия**: `v2.0.0` (после оптимизации на 65%)

**Особенности:**
- Финальная версия после оптимизации проекта
- Удалено 29 устаревших/дублирующихся файлов
- Переход на базу данных вместо файлового хранения
- Полная документация в README.md
- 20 shell скриптов для работы с устройствами

**Использование**: Основная рабочая версия для продакшена

---

### Ветка `ansible-migration`

**Статус**: Разработка  
**Назначение**: Миграция на Ansible для управления конфигурацией  
**Последний коммит**: `071659ab` - "Добавлена финальная сводка оптимизаций"

**Особенности:**
- Работа по переходу от shell-скриптов к Ansible playbooks
- Документация оптимизаций в FINAL_PUSH_SUMMARY.md
- Результаты тестирования (100% успешно)

**Использование**: Для разработки и тестирования Ansible-подхода

---

### Ветка `caplite-integration`

**Статус**: Разработка  
**Назначение**: Интеграция с Caplite системой  
**Последний коммит**: `3a3370df` - "Add Makefile and comprehensive README"

**Особенности:**
- Добавлен Makefile для удобного управления проектом
- Улучшенная документация README.md
- Команды для мониторинга статуса и логирования

**Использование**: Для интеграции с Caplite системой

---

### Ветка `web-interface-improvements`

**Статус**: Разработка  
**Назначение**: Улучшения веб-интерфейса проекта  
**Последний коммит**: `09d10315` - "Add TFTP server setup to deployment script"

**Особенности:**
- Добавлена настройка TFTP сервера в deployment скрипт
- Улучшения веб-интерфейса

**Использование**: Для разработки веб-интерфейса

---

## 🏷️ Теги проекта

### Тег `v2.0.0`

**Описание**: Major cleanup release - 65% project optimization

**Основные изменения:**
- Удалено 29 избыточных/устаревших файлов
- Устранены все дубликаты и legacy код
- Оптимизирована структура проекта
- Реализован подход "database-first"
- Добавлена полная документация

**Целевой коммит**: `5a7420b2` - "Final cleanup: Remove legacy files and duplicates"

**Статус**: Стабильный релиз, рекомендуется для использования

---

### Тег `v1.0.0`

**Описание**: Initial stable release - Network Device Configurator

**Основные изменения:**
- Первая стабильная версия проекта
- Добавлены служебные скрипты
- Базовая функциональность

**Целевой коммит**: `ae478d14` - "Add service and utility scripts"

**Статус**: Исходная версия, рекомендуется использовать v2.0.0

---

## 📁 Структура проекта

```
network-device-configurator/
├── 📋 config.env.example       # Шаблон конфигурации (ОБЯЗАТЕЛЬНО настроить!)
├── 📋 README.md                # Основная документация проекта
├── 📁 device/                  # Скрипты для устройств
│   ├── 📁 mavr/                # Устройства МАВР (8 файлов)
│   │   ├── configure.sh        # ГЛАВНОЕ МЕНЮ МАВР
│   │   ├── grandstream_HT812.sh # Настройка VoIP адаптеров
│   │   ├── wifi_mikrotik.sh    # Настройка WiFi точек
│   │   ├── grand_check.sh      # Проверка статуса устройств
│   │   ├── grand_reboot.sh     # Перезагрузка Grandstream
│   │   └── scan_wifi_mikrotik_chek.sh # Сканер MAVR WiFi
│   │
│   ├── 📁 upc_planet/          # Устройства UPC Planeta (12 файлов)
│   │   ├── menu_upc_planet.sh  # ГЛАВНОЕ МЕНЮ UPC
│   │   ├── start_fmtd.sh       # ОСНОВНОЙ СКРИПТ с БД
│   │   ├── grandstream_HT812.sh # Настройка Grandstream
│   │   ├── wifi_mikrotik.sh    # Настройка WiFi
│   │   ├── grand_check.sh      # Проверка Grandstream
│   │   ├── mikrotik_check.sh   # Проверка MikroTik
│   │   └── insert.sh           # Вставка в БД
│   │
│   └── 📁 upc_mikrotik/        # MikroTik устройства (1 файл)
│       └── mikrotik_rn1.sh     # Настройка MikroTik RN1
│
├── 📁 service/                 # Служебные скрипты (8 файлов)
│   ├── dhcp_scan.sh            # Мониторинг DHCP-аренды
│   └── insert.sh               # Запись в базу данных
│
└── 📁 docs/                    # Документация
    ├── installation.md         # Руководство по установке
    └── database-schema.sql     # Схема базы данных
```

---

## 🔧 Основные компоненты

### 1. Конфигурация (`config.env.example`)

**Назначение**: Шаблон конфигурационного файла с настройками проекта

**Обязательные параметры для изменения:**
- `YOUR_MIKROTIK_IP` → IP адрес главного MikroTik (DHCP сервер)
- `YOUR_NETWORK_PATTERN` → Паттерн подсети для DHCP сканирования (например: "10.19.11")
- `/path/to/scripts/` → Реальный путь к проекту на сервере

**Примеры конфигурации для разных сетей:**
- Корпоративная сеть 10.19.x.x: `dhcp_list_grep="10.19.11.*..:.."`
- Домашняя сеть 192.168.1.x: `dhcp_list_grep="192.168.1.*..:.."`
- Офисная сеть 172.16.x.x: `dhcp_list_grep="172.16.0.*..:.."`

**ВАЖНО**: Скопировать `config.env.example` в `config.env` и настроить реальные значения.  
**БЕЗОПАСНОСТЬ**: Никогда не коммитить `config.env` с реальными данными в Git!

---

### 2. База данных (MySQL)

**Таблица**: `devices.reg_prod`

**Структура:**
```sql
CREATE TABLE devices.reg_prod (
    id INT AUTO_INCREMENT PRIMARY KEY,
    prod VARCHAR(50),              -- Тип продукта (UPS, MAVR)
    dev VARCHAR(50),                 -- Тип устройства (Grandstream HT812, Mikrotik AP)
    mac_lan VARCHAR(17),             -- MAC-адрес LAN интерфейса
    mac_wan VARCHAR(17),             -- MAC-адрес WAN интерфейса
    ip_wan VARCHAR(15),              -- IP-адрес устройства
    chk TINYINT DEFAULT 0,           -- Статус проверки (0/1)
    datecfg DATE,                    -- Дата конфигурации
    timecfg TIME                     -- Время конфигурации
);
```

**Назначение**: Хранение информации о сконфигурированных устройствах вместо файлового лога

---

### 3. Основные скрипты

#### Для UPC Planeta:
```bash
./device/upc_planet/menu_upc_planet.sh     # Главное меню
./device/upc_planet/start_fmtd.sh          # Основной скрипт с БД
./device/upc_planet/grand_check.sh         # Проверка Grandstream
./device/upc_planet/mikrotik_check.sh      # Проверка MikroTik
```

#### Для МАВР:
```bash
./device/mavr/configure.sh                 # Главное меню МАВР
./device/mavr/grand_check.sh               # Проверка устройств
./device/mavr/grand_reboot.sh              # Перезагрузка Grandstream
```

#### Служебные:
```bash
./service/dhcp_scan.sh                     # Мониторинг DHCP-аренды (фоновый режим)
```

---

## 🚀 Как работает проект

### Архитектура

```
MikroTik (DHCP Server) ← SSH → NDC Scripts → TFTP Server
                                    ↓
                              MySQL Database
```

### Принцип работы

1. **Сканирование** - получение списка устройств из DHCP MikroTik через SSH
2. **Фильтрация** - выбор нужных устройств по MAC-адресам (префиксы: C0:74:AD для Grandstream, DC:2C:6E для MikroTik)
3. **Подключение** - SSH к устройству для получения информации
4. **Конфигурация** - создание и загрузка конфигурационных файлов через TFTP
5. **Применение** - перезагрузка устройства с новой конфигурацией
6. **Запись** - сохранение результатов в БД (таблица `devices.reg_prod`)
7. **Проверка** - контроль успешности настройки

---

## 📝 Документация

### Основная документация

- **README.md** - Полное описание проекта, установка, использование
- **docs/installation.md** - Руководство по установке
- **docs/database-schema.sql** - Схема базы данных
- **config.env.example** - Шаблон конфигурации с подробными комментариями

### История изменений

Проект прошел значительную оптимизацию:
- **v1.0.0** → **v2.0.0**: Удалено 29 файлов, оптимизация на 65%
- Переход от файлового хранения к базе данных
- Устранение дубликатов и legacy кода
- Улучшение документации

---

## 🔑 Основные данные для работы

### Обязательные настройки

1. **Создать `config.env`** из `config.env.example`
2. **Настроить MySQL** - создать базу `devices` и таблицу `reg_prod`
3. **Настроить TFTP** - директория `/srv/tftp` должна быть доступна
4. **SSH доступ** - к MikroTik устройству по SSH (пользователь `admin`)

### Зависимости

- MySQL/MariaDB
- SSH клиент
- expect (для автоматизации SSH)
- TFTP сервер (tftpd-hpa)

### Основные команды

```bash
# Установка зависимостей (Debian/Ubuntu)
sudo apt update
sudo apt install mysql-server expect ssh tftpd-hpa

# Настройка конфигурации
cp config.env.example config.env
nano config.env

# Запуск основных скриптов
./device/upc_planet/menu_upc_planet.sh    # Для UPC Planeta
./device/mavr/configure.sh                 # Для МАВР
```

---

## 📞 Информация для коллеги

### При принятии дел проверьте:

- [ ] Проект доступен в GitLab (`http://10.19.1.20/Automatization/network-device-configurator`)
- [ ] Прочитан README.md проекта
- [ ] Настроен `config.env` (скопирован из `config.env.example`)
- [ ] Настроена база данных MySQL (`devices.reg_prod`)
- [ ] Настроен TFTP сервер (`/srv/tftp`)
- [ ] Есть SSH доступ к MikroTik устройству
- [ ] Понятны различия между ветками:
  - `main` - основная рабочая версия (v2.0.0)
  - `ansible-migration` - миграция на Ansible
  - `caplite-integration` - интеграция с Caplite
  - `web-interface-improvements` - улучшения веб-интерфейса
- [ ] Понятны теги:
  - `v2.0.0` - текущая стабильная версия (рекомендуется)
  - `v1.0.0` - исходная версия
- [ ] Знаете, как запускать основные скрипты
- [ ] Знаете, как проверять статус работы (логи, БД)

### Типичные операции

1. **Конфигурация новых устройств**: Запустить соответствующий меню-скрипт
2. **Проверка статуса**: Использовать `*_check.sh` скрипты
3. **Мониторинг DHCP**: Запустить `service/dhcp_scan.sh` в фоне
4. **Просмотр логов**: Проверить таблицу `devices.reg_prod` в БД

---

## ⚠️ Важные замечания

1. **Ветка `main` защищена** - нельзя напрямую пушить, только через Merge Request
2. **Не коммитить `config.env`** - только `config.env.example` в репозиторий
3. **База данных обязательна** - проект использует MySQL, не файловые логи
4. **SSH доступ к MikroTik** - должен быть настроен без пароля или с expect-скриптами

---

**Последнее обновление:** 2025-11-21  
**Версия документа:** 1.0  
**Статус:** Готово для передачи дел

